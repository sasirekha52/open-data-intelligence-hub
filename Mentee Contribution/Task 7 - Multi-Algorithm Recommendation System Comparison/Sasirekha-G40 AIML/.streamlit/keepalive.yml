name: Streamlit Keep Alive

on:
  schedule:
    # Runs automatically every 4 days at midnight
    - cron: '0 0 */4 * *'
  workflow_dispatch: # Allows you to trigger this manually from GitHub if needed

jobs:
  ping_app:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit Web application
        run: |
          # "https://multialgorithmrecommendationsystemgit-gu9v58tfxyyukvnq94a46h.streamlit.app/"
          curl -I "https://multialgorithmrecommendationsystemgit-gu9v58tfxyyukvnq94a46h.streamlit.app//"
